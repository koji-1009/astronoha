---
/**
 * Heatmap page — year-by-year speech frequency visualization.
 *
 * Page shell returns immediately. The heatmap data (D1 read + incremental
 * generation) is deferred to a Server Island.
 *
 * CRZ Layer 1-2: server-rendered shell.
 * Server Island handles D1 interaction and auto-refresh.
 */

import DeferredHeatmap from "@/features/search/components/DeferredHeatmap.astro";
import LlmPanel from "@/features/search/components/LlmPanel";
import { ui } from "@/i18n/ui";
import BaseLayout from "@/shared/layout/BaseLayout.astro";

const keywordParam = Astro.params.keyword;
if (!keywordParam) {
	return Astro.redirect("/");
}
const decodedKeyword = decodeURIComponent(keywordParam);

const targetParam = Astro.url.searchParams.get("target");
const target: "kokkai" | "teikoku" | "both" =
	targetParam === "kokkai" ||
	targetParam === "teikoku" ||
	targetParam === "both"
		? targetParam
		: "kokkai";

const targetLabel =
	target === "both"
		? ui.search.targetBoth
		: target === "kokkai"
			? ui.search.targetKokkai
			: ui.search.targetTeikoku;
---

<BaseLayout
	title={`「${decodedKeyword}」の${ui.heatmap.title}`}
	description={`「${decodedKeyword}」の年代別出現頻度ヒートマップ`}
>
	<div class="heatmap-page">
		<nav class="breadcrumb" aria-label={ui.nav.breadcrumb}>
			<a href="/">{ui.nav.top}</a>
			<span class="breadcrumb-separator" aria-hidden="true">/</span>
			<a href={`/word/${encodeURIComponent(decodedKeyword)}`}>
				{ui.search.resultsTitle(decodedKeyword)}
			</a>
			<span class="breadcrumb-separator" aria-hidden="true">/</span>
			<span aria-current="page">{ui.heatmap.title}</span>
		</nav>

		<header class="page-header">
			<h1 class="page-title">「{decodedKeyword}」の{ui.heatmap.title}</h1>
			<nav class="target-chips" aria-label={ui.search.targetLabel}>
				{(["kokkai", "teikoku", "both"] as const).map((t) => {
					const label = t === "both" ? ui.search.targetBoth : t === "kokkai" ? ui.search.targetKokkai : ui.search.targetTeikoku;
					const href = t === "kokkai"
						? `/word/${encodeURIComponent(decodedKeyword)}/heatmap`
						: `/word/${encodeURIComponent(decodedKeyword)}/heatmap?target=${t}`;
					return target === t
						? <span class="target-chip active" aria-current="true">{label}</span>
						: <a href={href} class="target-chip">{label}</a>;
				})}
			</nav>
		</header>

		<DeferredHeatmap server:defer keyword={decodedKeyword} target={target}>
			<div slot="fallback" class="progress-container card">
				<p class="progress-label">ヒートマップデータを読み込み中…</p>
			</div>
		</DeferredHeatmap>

		<section class="llm-section">
			<LlmPanel client:idle actions={["trend"]} keyword={decodedKeyword} />
		</section>
	</div>
</BaseLayout>

<style>
.heatmap-page {
	display: flex;
	flex-direction: column;
	gap: var(--md-sys-spacing-6);
}

.breadcrumb {
	display: flex;
	flex-wrap: wrap;
	gap: var(--md-sys-spacing-1);
	align-items: center;
	font-size: var(--md-sys-typescale-label-medium-size);
	color: var(--md-sys-color-on-surface-variant);
}

.breadcrumb a {
	color: var(--md-sys-color-primary);
	text-decoration: none;
}

.breadcrumb a:hover {
	text-decoration: underline;
}

.breadcrumb-separator {
	color: var(--md-sys-color-outline);
}

.page-header {
	display: flex;
	flex-direction: column;
	gap: var(--md-sys-spacing-3);
}

.page-title {
	font-size: var(--md-sys-typescale-headline-medium-size);
	line-height: var(--md-sys-typescale-headline-medium-line-height);
	font-weight: var(--md-sys-typescale-headline-medium-weight);
	color: var(--md-sys-color-on-surface);
}

.target-chips {
	display: flex;
	gap: var(--md-sys-spacing-2);
}

.target-chip {
	display: inline-flex;
	align-items: center;
	padding: var(--md-sys-spacing-1) var(--md-sys-spacing-3);
	border-radius: var(--md-sys-shape-corner-small);
	font-size: var(--md-sys-typescale-label-large-size);
	font-weight: var(--md-sys-typescale-label-large-weight);
	text-decoration: none;
	border: 1px solid var(--md-sys-color-outline);
	color: var(--md-sys-color-on-surface-variant);
	background: transparent;
}

.target-chip:hover {
	background: color-mix(
		in srgb,
		var(--md-sys-color-on-surface) 8%,
		transparent
	);
}

.target-chip.active {
	background: var(--md-sys-color-secondary-container);
	color: var(--md-sys-color-on-secondary-container);
	border-color: transparent;
}

.progress-container {
	display: flex;
	flex-direction: column;
	gap: var(--md-sys-spacing-2);
}

.progress-label {
	font-size: var(--md-sys-typescale-body-medium-size);
	color: var(--md-sys-color-on-surface-variant);
}

@media (max-width: 599px) {
	.page-title {
		font-size: var(--md-sys-typescale-headline-small-size);
		line-height: var(--md-sys-typescale-headline-small-line-height);
	}
}
</style>
