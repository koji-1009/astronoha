---
/**
 * 500 error page.
 *
 * CRZ Layer 1: static HTML, no JavaScript needed.
 * Displayed when an unhandled server error occurs.
 * Receives `error` prop from Astro (4.11+).
 */

import { ui } from "@/i18n/ui";
import BaseLayout from "@/shared/layout/BaseLayout.astro";

interface Props {
	error: unknown;
}

const { error } = Astro.props;

// Log full error (including stack trace) to server logs.
// Dev: Astro also logs to terminal automatically (this duplicates).
// Production (Cloudflare Workers): this is the only log output
// — visible via `wrangler tail` or the dashboard.
console.error(error);

const errorMessage =
	error instanceof Error ? error.message : ui.error.unknownError;
---

<BaseLayout
	title={ui.error.serverErrorTitle}
	description={ui.error.serverErrorDescription}
>
	<div class="error-page">
		<div class="error-content">
			<p class="error-code">500</p>
			<h1 class="error-title">{ui.error.serverErrorTitle}</h1>
			<p class="error-description">{ui.error.serverErrorDescription}</p>
			<p class="error-detail">{errorMessage}</p>
			<div class="error-actions">
				<a href="/" class="action-link">{ui.nav.backToTop}</a>
			</div>
		</div>
	</div>
</BaseLayout>

<style>
.error-page {
	display: flex;
	align-items: center;
	justify-content: center;
	min-height: calc(100dvh - 300px);
	text-align: center;
}

.error-content {
	display: flex;
	flex-direction: column;
	align-items: center;
	gap: var(--md-sys-spacing-4);
	max-width: 480px;
}

.error-code {
	font-size: var(--md-sys-typescale-display-large-size);
	line-height: var(--md-sys-typescale-display-large-line-height);
	font-weight: var(--md-sys-typescale-display-large-weight);
	color: var(--md-sys-color-error);
}

.error-title {
	font-size: var(--md-sys-typescale-headline-medium-size);
	line-height: var(--md-sys-typescale-headline-medium-line-height);
	font-weight: var(--md-sys-typescale-headline-medium-weight);
	color: var(--md-sys-color-on-surface);
}

.error-description {
	font-size: var(--md-sys-typescale-body-large-size);
	line-height: var(--md-sys-typescale-body-large-line-height);
	color: var(--md-sys-color-on-surface-variant);
}

.error-detail {
	font-size: var(--md-sys-typescale-body-small-size);
	line-height: var(--md-sys-typescale-body-small-line-height);
	color: var(--md-sys-color-outline);
	word-break: break-all;
}

.error-actions {
	margin-top: var(--md-sys-spacing-4);
}

.action-link {
	display: inline-flex;
	align-items: center;
	justify-content: center;
	height: 40px;
	padding: 0 var(--md-sys-spacing-6);
	border-radius: var(--md-sys-shape-corner-full);
	background: var(--md-sys-color-primary);
	color: var(--md-sys-color-on-primary);
	font-size: var(--md-sys-typescale-label-large-size);
	font-weight: var(--md-sys-typescale-label-large-weight);
	text-decoration: none;
	transition: box-shadow 0.2s;
}

.action-link:hover {
	box-shadow: var(--md-sys-elevation-1);
}

@media (max-width: 599px) {
	.error-code {
		font-size: var(--md-sys-typescale-display-medium-size);
		line-height: var(--md-sys-typescale-display-medium-line-height);
	}

	.error-title {
		font-size: var(--md-sys-typescale-headline-small-size);
		line-height: var(--md-sys-typescale-headline-small-line-height);
	}
}
</style>
